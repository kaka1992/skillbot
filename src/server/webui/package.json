{
  "name": "claude-server-webui",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --minify && cp index.html dist/",
    "watch": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --watch",
    "start": "node server.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
